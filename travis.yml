sudo: required

env:
  global:
    - DOCKER_REPO=docker/repo
    - EB_REGION="us-east-1"
    - secure: $AWS_ACCOUNT_ID
    - secure: $AWS_ACCESS_KEY_ID
    - secure: $AWS_SECRET_ACCESS_KEY
    - COMMIT=${TRAVIS_COMMIT::8}


jobs:
  include:
    - name: GitGuardian Scan
      language: python
      python: 3.8

      install:
        - pip install ggshield
      script:
        - ggshield scan ci

    - name: security tests
      language: node_js
      node_js:
         - "14"
      
      dist: trusty

      services:
         - docker

      before_install:
         - export PATH=$PATH:$HOME/.local/bin
         - docker build -t trivy-ci-test:${COMMIT} .
         - export VERSION=$(curl --silent "https://api.github.com/repos/aquasecurity/trivy/releases/latest" | grep '"tag_name":' | sed -E 's/.*"v([^"]+)".*/\1/')
         - wget https://github.com/aquasecurity/trivy/releases/download/v${VERSION}/trivy_${VERSION}_Linux-64bit.tar.gz
         - tar zxvf trivy_${VERSION}_Linux-64bit.tar.gz
         - docker build -t dockle-ci-test:${COMMIT} .
         - export VERSION=$(curl --silent "https://api.github.com/repos/goodwithtech/dockle/releases/latest" | grep '"tag_name":' | sed -E 's/.*"v([^"]+)".*/\1/')
         - wget https://github.com/goodwithtech/dockle/releases/download/v${VERSION}/dockle_${VERSION}_Linux-64bit.tar.gz
         - tar zxvf dockle_${VERSION}_Linux-64bit.tar.gz

      before_script:
         - npm install
      script:
         - ./trivy --exit-code 0 --severity HIGH --no-progress trivy-ci-test:${COMMIT}
         - ./trivy --exit-code 1 --severity CRITICAL --no-progress trivy-ci-test:${COMMIT}
         - ./dockle --exit-code 0 dockle-ci-test:${COMMIT}
       
      after_success:
         - docker --version  # document the version travis is using
         - pip install --user awscli # install aws cli w/o sudo
         - export PATH=$PATH:$HOME/.local/bin # put aws in the path
         - eval $(aws ecr get-login --region us-east-1) #needs AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY envvars
         - docker build -t docker/repo .
         - docker tag docker/repo:latest 335319979316.dkr.ecr.us-east-1.amazonaws.com/docker/repo:latest
         - docker push 335319979316.dkr.ecr.us-east-1.amazonaws.com/docker/repo:latest
      
      cache:
         directories:
           - $HOME/.cache/trivy



