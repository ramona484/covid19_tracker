worker-processes auto;

events {
    worker_connections 1024;
    use epoll;
    }

http {
    upstream app {
        server app: 80;
    }

server {

    listen 80;

    location / {
        proxy_pass http://app;
        }
}
